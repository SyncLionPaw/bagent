import{_ as a,o as s,c as t,a2 as n}from"./chunks/framework.DGu13UIe.js";const g=JSON.parse('{"title":"第 12 课 · 本地极小模型推理（transformers）","description":"","frontmatter":{"editLink":"https://github.com/SyncLionPaw/bagent/edit/main/lessons/12-local-inference/doc.md"},"headers":[],"relativePath":"chapters/12-local-inference.md","filePath":"chapters/12-local-inference.md"}'),o={name:"chapters/12-local-inference.md"};function r(i,e,l,c,d,h){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="第-12-课-·-本地极小模型推理-transformers" tabindex="-1">第 12 课 · 本地极小模型推理（transformers） <a class="header-anchor" href="#第-12-课-·-本地极小模型推理-transformers" aria-label="Permalink to &quot;第 12 课 · 本地极小模型推理（transformers）&quot;">​</a></h1><p><strong>约 30 分钟</strong> · 第 11 课之后：<strong>在本机跑一小段 forward</strong>，感受加载与生成耗时</p><h2 id="本课用-python-例外说明" tabindex="-1">本课用 Python（例外说明） <a class="header-anchor" href="#本课用-python-例外说明" aria-label="Permalink to &quot;本课用 Python（例外说明）&quot;">​</a></h2><p>课程主线仍是 <strong>JavaScript / Node</strong>。<br> 这一课用 <strong><a href="https://huggingface.co/docs/transformers" target="_blank" rel="noreferrer">Hugging Face <code>transformers</code></a></strong> 在本机下载极小权重并 <code>generate</code>。</p><ul><li>不需要 <code>DEEPSEEK_API_KEY</code></li><li><strong>权重只落在本课目录 <code>models/</code></strong>，不写入 <code>~/.cache/huggingface</code>；随时 <code>rm -rf models</code> 清空</li><li>虚拟环境优先 <strong><a href="https://docs.astral.sh/uv/" target="_blank" rel="noreferrer">uv</a></strong>，没有则回退 <code>python3 -m venv</code></li><li><strong>两套安装</strong>：CPU 版 / CUDA 版 PyTorch（二选一）</li></ul><hr><h2 id="和第-11-课的关系" tabindex="-1">和第 11 课的关系 <a class="header-anchor" href="#和第-11-课的关系" aria-label="Permalink to &quot;和第 11 课的关系&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>第 11 课（云端）</th><th>第 12 课（本地）</th></tr></thead><tbody><tr><td>权重在哪</td><td>厂商机房</td><td><strong><code>lessons/12-local-inference/models/</code></strong></td></tr><tr><td>你怎么调用</td><td><code>fetch</code> → JSON</td><td><code>transformers</code> → <code>model.generate</code></td></tr><tr><td>计时看什么</td><td>网络 + 机房推理</td><td><strong>下载到本目录</strong> + <strong>加载</strong> + <strong>本机 forward</strong></td></tr></tbody></table><hr><h2 id="用什么模型" tabindex="-1">用什么模型 <a class="header-anchor" href="#用什么模型" aria-label="Permalink to &quot;用什么模型&quot;">​</a></h2><p><a href="https://huggingface.co/sshleifer/tiny-gpt2" target="_blank" rel="noreferrer"><code>sshleifer/tiny-gpt2</code></a>：体积极小，适合教学计时。<br><strong>不是</strong>产品级对话模型；只用来建立「推理要花时间」的体感。</p><hr><h2 id="_1-安装依赖-先做一次" tabindex="-1">1. 安装依赖（先做一次） <a class="header-anchor" href="#_1-安装依赖-先做一次" aria-label="Permalink to &quot;1. 安装依赖（先做一次）&quot;">​</a></h2><p>在仓库根目录：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 无 NVIDIA GPU / Mac / 只想 CPU</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ch12:setup:cpu</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 有 NVIDIA GPU + 驱动（CUDA 12.x 线）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ch12:setup:cuda</span></span></code></pre></div><p>脚本 <a href="https://github.com/SyncLionPaw/bagent/blob/main/lessons/12-local-inference/setup.sh" target="_blank" rel="noreferrer"><code>setup.sh</code></a> 会：</p><ol><li>有 <code>uv</code> → <code>uv venv .venv</code>；否则 → <code>python3 -m venv .venv</code></li><li>用 <strong>PyTorch 官方索引</strong> 安装对应版本： <ul><li><strong>cpu</strong>：<code>https://download.pytorch.org/whl/cpu</code></li><li><strong>cuda</strong>：<code>https://download.pytorch.org/whl/cu124</code></li></ul></li><li>再装 <code>transformers</code></li><li>写入 <code>.venv-variant</code>（<code>cpu</code> 或 <code>cuda</code>），方便你对照日志</li></ol><p>也可在本课目录手动执行：<code>./setup.sh cpu</code> 或 <code>./setup.sh cuda</code>（需 <code>chmod +x setup.sh run.sh</code>）。</p><table tabindex="0"><thead><tr><th>版本</th><th>何时选</th></tr></thead><tbody><tr><td><strong>cpu</strong></td><td>默认；Mac、无独显、或只想先跑通</td></tr><tr><td><strong>cuda</strong></td><td>机器有 NVIDIA GPU 且驱动正常；<code>run.py</code> 里 <code>torch.cuda.is_available()</code> 为真才会用 GPU</td></tr></tbody></table><p>装过 CUDA 版但检测不到 GPU 时，脚本仍会跑，只是自动落在 <strong>cpu</strong> 上。</p><hr><h2 id="_2-权重下载位置" tabindex="-1">2. 权重下载位置 <a class="header-anchor" href="#_2-权重下载位置" aria-label="Permalink to &quot;2. 权重下载位置&quot;">​</a></h2><p><code>run.py</code> 启动时设置：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lessons/12-local-inference/models/     ← HF_HOME</span></span>
<span class="line"><span>lessons/12-local-inference/models/hub/ ← 实际 hub 缓存</span></span></code></pre></div><p><strong>删掉整个课就不占磁盘：</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -rf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lessons/12-local-inference/models</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -rf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lessons/12-local-inference/.venv</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 连环境一起删</span></span></code></pre></div><hr><h2 id="_3-运行" tabindex="-1">3. 运行 <a class="header-anchor" href="#_3-运行" aria-label="Permalink to &quot;3. 运行&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ch12</span></span></code></pre></div><p>内部调用 <a href="https://github.com/SyncLionPaw/bagent/blob/main/lessons/12-local-inference/run.sh" target="_blank" rel="noreferrer"><code>run.sh</code></a> → <code>.venv/bin/python run.py</code>。</p><p>第一次会联网把 <code>tiny-gpt2</code> 拉到 <code>models/</code>；之后再跑主要是加载 + 推理时间。</p><p>示例输出（数字因机器而异）：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>权重目录: .../lessons/12-local-inference/models</span></span>
<span class="line"><span>设备: cpu（安装时选了 cpu）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>加载 tokenizer: 0.xx s</span></span>
<span class="line"><span>加载模型权重:   0.xx s</span></span>
<span class="line"><span>推理耗时: 0.xxx s（新生成约 32 tokens）</span></span>
<span class="line"><span>约 xx.x tokens/s（cpu，极小模型，仅作体感）</span></span></code></pre></div><hr><h2 id="代码在干什么" tabindex="-1">代码在干什么 <a class="header-anchor" href="#代码在干什么" aria-label="Permalink to &quot;代码在干什么&quot;">​</a></h2><p><a href="https://github.com/SyncLionPaw/bagent/blob/main/lessons/12-local-inference/run.py" target="_blank" rel="noreferrer"><code>run.py</code></a>：</p><ol><li>把缓存指到本课 <code>models/</code></li><li><code>from_pretrained(..., cache_dir=...)</code> 加载 tokenizer / 模型</li><li><code>.to(device)</code> 后 <code>generate</code>（<code>device</code> 由 CUDA 是否可用决定）</li><li>分别计时「加载」与「推理」，打印粗略 tokens/s</li></ol><p><code>do_sample=False</code>：贪心解码，方便重复对比耗时。</p><hr><h2 id="该怎么读这些数字" tabindex="-1">该怎么读这些数字 <a class="header-anchor" href="#该怎么读这些数字" aria-label="Permalink to &quot;该怎么读这些数字&quot;">​</a></h2><ul><li><strong>加载合计</strong>：从 <code>models/</code> 读入内存；云端 API 把这步藏在机房冷启动里。</li><li><strong>推理耗时</strong>：一次 <code>generate</code>；token 越多越久。</li><li><strong>tokens/s</strong>：仅作体感；真实产品会用 GPU 集群、vLLM 等把吞吐拉高很多。</li></ul><hr><h2 id="检查点" tabindex="-1">检查点 <a class="header-anchor" href="#检查点" aria-label="Permalink to &quot;检查点&quot;">​</a></h2><ul><li>[ ] 会用 <code>npm run ch12:setup:cpu</code> 或 <code>:cuda</code> 装好 <code>.venv</code> 吗？</li><li>[ ] 知道权重在 <strong><code>lessons/12-local-inference/models/</code></strong>，而不是用户主目录默认缓存吗？</li><li>[ ] 能区分「加载权重」和「generate」两段耗时吗？</li><li>[ ] 能说清与第 11 课 <code>chat/completions</code> 的分工对应吗？</li></ul><h2 id="下一课" tabindex="-1">下一课 <a class="header-anchor" href="#下一课" aria-label="Permalink to &quot;下一课&quot;">​</a></h2><p><a href="/bagent/chapters/13-inference-engines.html">第 13 课 · vLLM / SGLang 等推理框架一览</a>（扩展阅读，无代码）。<br> 流式实践见 <a href="/bagent/chapters/14-fastapi-stream.html">第 14–16 课</a>。</p><p><a href="/bagent/chapters/11-inference.html">← 第 11 课</a> · <a href="https://docs.astral.sh/uv/" target="_blank" rel="noreferrer">uv</a> · <a href="https://huggingface.co/docs/transformers" target="_blank" rel="noreferrer">transformers</a> · <a href="https://huggingface.co/sshleifer/tiny-gpt2" target="_blank" rel="noreferrer">tiny-gpt2</a></p>`,47)])])}const u=a(o,[["render",r]]);export{g as __pageData,u as default};
