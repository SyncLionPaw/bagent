import{_ as e,o as a,c as t,a2 as i}from"./chunks/framework.DGu13UIe.js";const g=JSON.parse('{"title":"维护文档站","description":"","frontmatter":{},"headers":[],"relativePath":"development.md","filePath":"development.md"}'),n={name:"development.md"};function o(d,s,l,c,p,r){return a(),t("div",null,[...s[0]||(s[0]=[i(`<h1 id="维护文档站" tabindex="-1">维护文档站 <a class="header-anchor" href="#维护文档站" aria-label="Permalink to &quot;维护文档站&quot;">​</a></h1><h2 id="目录" tabindex="-1">目录 <a class="header-anchor" href="#目录" aria-label="Permalink to &quot;目录&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lessons/NN-xxx/     # 课文 doc.md + 示例代码（源）</span></span>
<span class="line"><span>docs/chapters/      # 构建时由 sync-docs 生成，勿手改</span></span>
<span class="line"><span>docs/guide/         # 站点通用页</span></span>
<span class="line"><span>scripts/sync-docs.mjs</span></span>
<span class="line"><span>scripts/sync-brand.py</span></span>
<span class="line"><span>.github/workflows/docs.yml</span></span></code></pre></div><h2 id="命令" tabindex="-1">命令 <a class="header-anchor" href="#命令" aria-label="Permalink to &quot;命令&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync-brand</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # image.png → docs/public（favicon / logo）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync-docs</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # lessons → docs/chapters</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span></code></pre></div><p>站点图标在 <code>docs/public/</code>（<code>favicon.ico</code>、<code>logo-icon.png</code> 等），源图是仓库根目录 <code>image.png</code>。改图后跑 <code>npm run sync-brand</code> 再 build。</p><p>安装指南截图源文件是根目录 <code>install.png</code>，<code>sync-docs</code> 会复制到 <code>docs/public/install.png</code>。</p><p>新增一章：</p><ol><li>建 <code>lessons/NN-xxx/doc.md</code> + 示例代码</li><li><code>npm run sync-docs</code>（会删除 <code>docs/chapters/</code> 里已无对应 lesson 的陈旧页面）</li><li>改 <code>docs/.vitepress/config.mts</code> 侧栏</li><li>根目录 <code>package.json</code> 加 <code>scripts</code>（如 <code>ch36</code>、<code>ch102:compile</code>）</li><li><code>npm run build</code> 确认无 dead link</li></ol><p>扩展阅读课（如 101、102）只写 <code>doc.md</code> 即可；有代码 demo 的课（如 102）另加 <code>package.json</code> 与 <code>src/</code>。</p><h2 id="github-自动构建与发布" tabindex="-1">GitHub 自动构建与发布 <a class="header-anchor" href="#github-自动构建与发布" aria-label="Permalink to &quot;GitHub 自动构建与发布&quot;">​</a></h2><p>与 <a href="https://github.com/SyncLionPaw/pagent" target="_blank" rel="noreferrer">pagent</a> 一样：<code>.github/workflows/docs.yml</code> 在 <strong>push <code>main</code></strong> 时跑 Actions，把 <code>docs/.vitepress/dist</code> 推到 <strong><code>gh-pages</code> 分支</strong>，由 GitHub Pages 对外提供静态站。</p><p>线上地址（项目页路径与 <code>docs/.vitepress/config.mts</code> 里 <code>base: &quot;/bagent/&quot;</code> 一致）：</p><p><strong><a href="https://synclionpaw.github.io/bagent/" target="_blank" rel="noreferrer">https://synclionpaw.github.io/bagent/</a></strong></p><h3 id="你需要在-github-上配置什么" tabindex="-1">你需要在 GitHub 上配置什么 <a class="header-anchor" href="#你需要在-github-上配置什么" aria-label="Permalink to &quot;你需要在 GitHub 上配置什么&quot;">​</a></h3><p>按顺序做一次即可（<strong>不用</strong>在 Secrets 里填 PyPI token 之类；文档部署只用内置 <code>GITHUB_TOKEN</code>）。</p><table tabindex="0"><thead><tr><th>步骤</th><th>位置</th><th>设置</th></tr></thead><tbody><tr><td>1</td><td>仓库 <strong>Settings → Actions → General</strong></td><td><strong>Workflow permissions</strong> 选 <strong>Read and write permissions</strong>（允许 workflow 往 <code>gh-pages</code> 推代码）。若组织仓库被策略锁住，需组织管理员放开。</td></tr><tr><td>2</td><td>仓库 <strong>Settings → Pages</strong></td><td><strong>Build and deployment → Source</strong> 选 <strong>Deploy from a branch</strong>；<strong>Branch</strong> 选 <strong><code>gh-pages</code></strong>，文件夹 <strong><code>/ (root)</code></strong>。不要选 “GitHub Actions” 作为 Pages 源（本仓库用 peaceiris 写 <code>gh-pages</code> 分支，与 pagent 相同）。</td></tr><tr><td>3</td><td>本地</td><td>确认已提交：<code>package-lock.json</code>、根目录 <code>image.png</code>、<code>.github/workflows/docs.yml</code>、<code>docs/public/</code> 可省略（CI 会从 <code>image.png</code> 生成）。</td></tr><tr><td>4</td><td>本地</td><td><code>git push origin main</code>（或合并进 <code>main</code> 的 PR 合并后）</td></tr><tr><td>5</td><td>仓库 <strong>Actions</strong> 页</td><td>打开 <strong>Docs</strong> workflow，等绿灯；首次成功后 <strong>Settings → Pages</strong> 会显示站点 URL。</td></tr></tbody></table><p>手动重跑：Actions → <strong>Docs</strong> → <strong>Run workflow</strong>（<code>workflow_dispatch</code>）。</p><h3 id="与-pagent-的差异" tabindex="-1">与 pagent 的差异 <a class="header-anchor" href="#与-pagent-的差异" aria-label="Permalink to &quot;与 pagent 的差异&quot;">​</a></h3><table tabindex="0"><thead><tr><th></th><th>pagent</th><th>bagent</th></tr></thead><tbody><tr><td><code>package.json</code></td><td>在 <code>docs/</code> 下</td><td>在<strong>仓库根</strong></td></tr><tr><td>CI <code>npm ci</code></td><td><code>working-directory: docs</code></td><td>仓库根执行</td></tr><tr><td>构建前</td><td>仅 Node</td><td>Node + <strong>Python 3.12 + Pillow</strong>（<code>npm run build</code> 会跑 <code>sync-brand</code>）</td></tr><tr><td>其它 workflow</td><td><code>publish.yml</code>（PyPI）、<code>ruff.yml</code></td><td>暂无（纯 JS 课程，无 PyPI 包）</td></tr></tbody></table><h3 id="本地对照-ci" tabindex="-1">本地对照 CI <a class="header-anchor" href="#本地对照-ci" aria-label="Permalink to &quot;本地对照 CI&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ci</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pillow</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 与 CI 一致</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vitepress</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> preview</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 打开 http://localhost:4173/bagent/</span></span></code></pre></div><h2 id="发布插件-github-release" tabindex="-1">发布插件（GitHub Release） <a class="header-anchor" href="#发布插件-github-release" aria-label="Permalink to &quot;发布插件（GitHub Release）&quot;">​</a></h2><p>用户<strong>不用 clone / compile</strong>：从 <a href="https://github.com/SyncLionPaw/bagent/releases" target="_blank" rel="noreferrer">Releases</a> 下载 <code>bagent-plugin.vsix</code>，配 Key 即用。安装说明见 <a href="/bagent/guide/install-plugin.html">安装插件</a>。</p><p>维护者发版步骤：</p><ol><li>改 <code>lessons/53-diff-preview/package.json</code> 里的 <strong><code>version</code></strong></li><li>提交并 push 到 <code>main</code></li><li>打 tag 并推送（tag 与 version 对齐，例如 <code>v0.0.2</code>）：</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tag</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> v0.0.2</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> v0.0.2</span></span></code></pre></div><ol start="4"><li>Actions 里 <strong>Release</strong> workflow 会自动 <code>ch53:package</code>，上传 <code>bagent-plugin.vsix</code> 与带版本号的 <code>.vsix</code></li></ol><p>也可在 Actions 页 <strong>Run workflow</strong> 手动触发（需已有 tag 或会用到当前 ref；推荐仍用 tag 发版）。</p><p>本地试打：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ch53:package</span></span></code></pre></div><h3 id="常见问题" tabindex="-1">常见问题 <a class="header-anchor" href="#常见问题" aria-label="Permalink to &quot;常见问题&quot;">​</a></h3><ul><li><strong>404 / 空白页</strong>：Pages 是否指向 <strong><code>gh-pages</code> + root</strong>；<code>config.mts</code> 的 <code>base</code> 是否为 <code>/bagent/</code>（与仓库名一致）。</li><li><strong>图标还是旧的</strong>：确认 <code>image.png</code> 已进仓库；合并后看 Actions 是否成功；浏览器强刷缓存。</li><li><strong>Actions 报 permission denied</strong>：回到步骤 1 打开 <strong>Read and write</strong>。</li><li><strong>sync-brand 失败</strong>：CI 需要根目录 <code>image.png</code>；本地可先 <code>npm run sync-brand</code> 自检。</li></ul>`,33)])])}const k=e(n,[["render",o]]);export{g as __pageData,k as default};
