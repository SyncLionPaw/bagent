{
  "name": "bagent-lesson47",
  "displayName": "bagent 第 47 课",
  "description": "斜杠保存对话到 ~/.bagent/{项目}/chats/",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "vscode": "^1.94.0"
  },
  "categories": ["Other"],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "bagent47.open",
        "title": "bagent: 打开 Agent 面板（第 47 课）"
      }
    ],
    "viewsContainers": {
      "secondarySidebar": [
        {
          "id": "bagent47",
          "title": "bagent",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "views": {
      "bagent47": [
        {
          "type": "webview",
          "id": "bagent47.chat",
          "name": "Agent"
        }
      ]
    },
    "configuration": {
      "title": "bagent 第 47 课",
      "properties": {
        "bagent47.apiKeyPath": {
          "type": "string",
          "default": "~/.bagent/deepseek-api-key",
          "description": "DeepSeek API Key 文件路径"
        },
        "bagent47.tavilyApiKeyPath": {
          "type": "string",
          "default": "~/.bagent/tavily-api-key",
          "description": "Tavily API Key（web_search）。第一行 tvly-...；环境变量 TAVILY_API_KEY 优先。"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p . && tsc -p agent/tsconfig.json"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.3"
  }
}
