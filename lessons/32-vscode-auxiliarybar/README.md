# bagent Agent（第 32 课）

VS Code / Cursor 扩展：在 **右侧辅助栏** spawn Agent 子进程，侧边栏流式聊天。

## 使用前

1. 创建 `~/.bagent/deepseek-api-key`（第一行 `sk-...`）
2. （可选）`~/.bagent/tavily-api-key` 以启用 `web_search`

## 打开面板

命令面板 → **bagent: 打开右侧 Agent 面板**

## 课程

[bagent 文档](https://synclionpaw.github.io/bagent/)
