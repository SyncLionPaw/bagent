# 怎么学

本课路线：**Agent**（1–10）→ **原理**（11–13）→ **假数据流**（14）→ **SSE 解析**（15）→ **真 API 流式**（16）。

- 一次只学一章，自己跑通 `npm run ch01` 这类命令  
- 代码在 `lessons/` 各章目录，不要只读文档  
- 约定：Node 20+、当章要求的环境变量（第 1 课 `DEEPSEEK_API_KEY`，第 9 课另要 `TAVILY_API_KEY`）

[环境](./environment) → … → [第 14 课](../chapters/14-fastapi-stream) → [第 15 课 解析](../chapters/15-sse-parse) → [第 16 课 流式](../chapters/16-streaming)
